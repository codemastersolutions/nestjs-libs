/**
 * Injection token for Better Auth before hook providers
 */
export const BETTER_AUTH_BEFORE_HOOK = Symbol('BETTER_AUTH_BEFORE_HOOK');

/**
 * Injection token for Better Auth after hook providers
 */
export const BETTER_AUTH_AFTER_HOOK = Symbol('BETTER_AUTH_AFTER_HOOK');

/**
 * Injection token for Better Auth hook providers
 */
export const BETTER_AUTH_HOOK = Symbol('BETTER_AUTH_HOOK');

/**
 * Injection token for Better Auth instance
 */
export const BETTER_AUTH_INSTANCE = Symbol('BETTER_AUTH_INSTANCE');

/**
 * Injection token for Better Auth configuration options
 */
export const BETTER_AUTH_OPTIONS = Symbol('BETTER_AUTH_OPTIONS');

/**
 * Injection token for Better Auth guard providers
 */
export const BETTER_AUTH_GUARD = Symbol('BETTER_AUTH_GUARD');
